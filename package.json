{
  "name": "thelab-membership",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "bootstrap": "bash scripts/bootstrap-worker.sh",
    "images:hero": "node scripts/generate-hero-images.js",
    "wiki:download": "node scripts/download-wiki.js",
    "wiki:import": "node scripts/import-wiki.js",
    "wiki:test-migration": "node --test scripts/wiki-migration.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "db:local": "wrangler d1 migrations apply thelab-membership --local"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "cheerio": "^1.2.0",
    "sharp": "^0.35.4",
    "turndown": "^7.2.4",
    "turndown-plugin-gfm": "^1.0.2",
    "vitest": "~4.1.0",
    "wrangler": "^4.130.0"
  },
  "overrides": {
    "sharp": "^0.35.4"
  },
  "dependencies": {
    "markdown-it": "^15.0.2",
    "qrcode-generator": "^2.0.4"
  }
}
